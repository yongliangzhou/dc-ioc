# dc-ioc-platform 上线就绪度评估

> 基于仓库实际代码核查（非文档罗列）。截至 2026-07-31。

## 总体结论

系统已完成大量生产化工作（认证/RBAC、CI/CD、多副本、监控、日志、密钥管控、登录限流、备份脚本、非 root 容器、资源限制）。但要真正"可上线"，仍有 **5 个 P0 阻塞项** 与若干 P1/P2 待办。下面按上线阻塞级别分级，并标注已确认具备的能力。

---

## P0 — 上线阻塞项（必须完成）

### 1. 缺少 TLS / 反向代理
- 生产栈 `deploy/docker-compose.prod.yml` 中前端 nginx 仅暴露 **HTTP :8080**，无 HTTPS 终止、无边缘 WAF / 限流。
- 生产流量必须经 HTTPS。需在前端前增加反向代理（nginx/Caddy/Traefik）或负载均衡 + 证书（ACME/内部 CA），并统一对外端口与 HSTS。

### 2. 数据库 Schema 管理割裂（init.sql 与 alembic 两套真相源）
- DB 首次启动靠 `init.sql`（docker-entrypoint-initdb.d）建表。
- 但仓库已有 4 个 Alembic 迁移（`0001_init_core_tables` … `0004_capacity_energy_history`），**部署流程中从未执行**：`backend/Dockerfile` 的 prod `CMD` 直接起 `uvicorn`，全局搜索 `alembic upgrade` 在部署脚本中 0 命中。
- 后果：init.sql 与 alembic 不是同一真相源；后续 schema 演进（加字段/索引）无受控路径，手动改库极易与代码漂移。
- 修复：统一为 Alembic（`alembic stamp head` + 启动 `alembic upgrade head`），`init.sql` 仅保留为开发种子或删除。

### 3. 生产数据来源未打通（无 Kafka / 上游采集链路）
- prod 默认 `EXTERNAL_MOCK_COLLECTOR_ENABLED=false`，但 compose 内**没有 Kafka 服务**；代码 `kafka_consumer.py` 依赖外部 `KAFKA_BOOTSTRAP_SERVERS` 与 `EXTERNAL_COLLECTOR_TOKEN`。
- 若上线前未先行部署 Kafka 集群或外部采集器管线，生产环境将**零遥测数据**（设备遥测、采集器接入页面为空）。
- 必须明确并落地上游采集链路（Kafka 部署 + topic + token 认证，或外部采集器接入），并做端到端联调。

### 4. 告警无通知出口（Alertmanager 被注释）
- `deploy/prometheus/prometheus.yml` 中 `alertmanagers` 配置被 `# 按需启用` 注释；`loki-config.yaml` 配了 `alertmanager_url` 但**无实际 Alertmanager 服务**。
- 告警规则（CPU/内存/磁盘/服务存活等）触发后无人接收。
- 修复：启用 Alertmanager + 路由（邮件/钉钉/Webhook），并做一次真实告警演练验证可达。

### 5. CI 跳过 DB / 集成测试
- `.github/workflows/ci.yml` 后端测试为 `pytest tests/ -k "not db and not integration"`，即**数据库与集成路径全部跳过**。
- 后端仅 3 个单测文件（`test_alarm_engine`、`test_phase2_core`、`test_twin_topology`），前端仅 2 个 spec（routerGuard、request）。
- 上线前需补充基于真实 DB 的集成测试，覆盖核心读写（外部设备、遥测、告警、工单、知识库导入等）与关键 API 契约。

---

## P1 — 强烈建议（上线前完成）

6. **CORS 硬编码** `localhost:8080`：生产用真实域名/CDN 时需参数化 `CORS_ORIGINS`。
7. **备份未自动化/未演练**：`pg_dump` 脚本存在，但 compose 中无定时任务；缺 offsite 备份与恢复演练，RTO/RPO 未定义。
8. **密钥注入机制未固化**：prod compose 用 `# 外部注入` 注释，但无 `.env` 加载说明；Grafana 管理员密码等需明确注入方式与轮换策略。
9. **PostgreSQL / Redis 单点**：无复制/托管，数据持久性依赖单卷+备份；建议托管 DB 或主从复制。
10. **前端残留 TS 类型错误**：`plan.md` 标记多个 `.vue` 文件（api/index.ts 的 mockData、Alarms/Drill/Equipment/DomainDevices/Pagination/CollectorDeviceForm 等）有未修复类型错误；不阻断构建但有运行期隐患，应清理。
11. **Docker 冒烟测试未实际执行**：`plan.md` 标记 ⬜，建议固化为部署后 hook。

---

## P2 — 上线后 / 增强项

12. **Celery 配置但未使用**：`CELERY_BROKER_URL` 配置但无 worker / 任务，属死配置（移除或实装后台任务）。
13. **限流仅登录端点**：其他 API 无限流，建议全局/按角色限流 + 防爆破扩展。
14. **负载测试基线未沉淀**：locust 存在但无记录容量/SLO 基线、无压测报告。
15. **安全扫描不阻断**：CI 中 bandit/safety/trivy 均 `|| true`；建议设基线并逐步清零高危。
16. **测试覆盖薄**：补充 API 契约测试与关键页面快照/交互测试。

---

## 已具备（已确认，可放心）

- 认证 + RBAC：登录/刷新/改密/角色接口齐备；前端 `router/index.ts` 守卫已接（未登录跳登录、路由级 requiredRoles 校验）。
- `secret_check.py` 启动时阻断默认/弱密钥。
- CI/CD：GitHub Actions 多阶段构建 + Ruff/Bandit/Safety 扫描 + Trivy 镜像扫描 + 多副本部署。
- 监控：`deploy/prometheus`（含 alert rules）+ `grafana` + `loki/promtail` 日志。
- 运维：备份脚本、资源限制、非 root、只读根（dev）；登录端点限流。

---

## 上线前最小行动清单（建议顺序）

1. 部署 Kafka/外部采集链路并端到端联调（P0-3）。
2. 统一 DB schema 管理为 Alembic（P0-2）。
3. 加 TLS 反向代理（P0-1）。
4. 启用 Alertmanager 并验证告警可达（P0-4）。
5. 补 DB 集成测试并纳入 CI（P0-5）。
6. CORS 参数化 + 密钥注入固化 + 备份自动化与恢复演练（P1）。
